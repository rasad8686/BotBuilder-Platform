/**
 * Communication - Export all communication classes
 */

const MessageBus = require('./MessageBus');
const ContextManager = require('./ContextManager');

module.exports = {
  MessageBus,
  ContextManager
};
