/**
 * Voice to Bot Services Export
 */

const VoiceProcessor = require('./VoiceProcessor');
const IntentExtractor = require('./IntentExtractor');
const BotGenerator = require('./BotGenerator');

module.exports = {
  VoiceProcessor,
  IntentExtractor,
  BotGenerator
};
