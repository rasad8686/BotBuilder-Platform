NODE_ENV=production
PORT=10000

# Render PostgreSQL Database URL
# Get this from: Render Dashboard → PostgreSQL Instance → Connection String
# Use EXTERNAL URL for local development
# Use INTERNAL URL for Render deployment (set in Environment Variables)
DATABASE_URL=postgresql://username:password@host.render.com/database_name

# JWT Secret for authentication tokens
# Generate a strong random string for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend URL for CORS configuration
FRONTEND_URL=https://bot-builder-platform.vercel.app
